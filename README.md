# nooprompter


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

[Nooprompter](https://civvic.github.io/nooprompter/) is a simple tool
for managing and generating LLM context files from templates. It helps
you maintain IDE rules, prompt templates, and meta-programming documents
with tag-based substitutions.

**Key Features:**  
- 🏷️ **Tag Substitution**: Simple `<Tag-Name>` format with recursive
support  
- 📁 **Multi-Vendor Support**: Generate rules for Cursor, Claude Coder,
and more  
- ⚙️ **Flexible Configuration**: YAML-based configuration with sensible
defaults  
- 🔍 **Tag Discovery**: Find all tags used in your templates  
- 🚀 **CLI Interface**: Easy-to-use command-line tools

Perfect for managing project-specific LLM rules, documentation
templates, and prompt libraries across different AI coding assistants.

## Usage

### Installation

Install latest from the GitHub
[repository](https://github.com/civvic/nooprompter):

``` sh
$ pip install git+https://github.com/civvic/nooprompter.git
```

or from [conda](https://anaconda.org/civvic/nooprompter)

``` sh
$ conda install -c civvic nooprompter
```

or from [pypi](https://pypi.org/project/nooprompter/)

``` sh
$ pip install nooprompter
```

### Documentation

Documentation can be found hosted on this GitHub
[repository](https://github.com/civvic/nooprompter)’s
[pages](https://civvic.github.io/nooprompter/). Additionally you can
find package manager specific guidelines on
[conda](https://anaconda.org/civvic/nooprompter) and
[pypi](https://pypi.org/project/nooprompter/) respectively.

## Quick Start

### 1. Initialize a new project

``` bash
nooprompter-init
```

This creates a `.nooprompter.yml` configuration file with examples.

### 2. Create template files

Create a `templates/` directory with your template files:

    templates/
    ├── Cursor/
    │   ├── project.mdc
    │   └── about-me.mdc
    └── meta/
        ├── HANDOFF.md
        └── STARTING_PROMPT.md

### 3. Add tags to your templates

Use `<Tag-Name>` format in your templates:

``` markdown
# Project: <Project-Name>

**Objective:** <Project-Objective>

**Developer:** <User-Alias>
```

### 4. Configure tags in `.nooprompter.yml`

``` yaml
tags:
  Project-Name: nooprompter
  Project-Objective: Manage LLM templates with tag substitution
  User-Alias: vic
```

### 5. Build your templates

``` bash
nooprompter  # Build all templates
```

Files will be generated in their configured destinations (e.g.,
`.cursor/rules/`, `meta/`).

## CLI Commands

### Build Templates

``` bash
# Build all templates
nooprompter

# Build specific vendor only
nooprompter --vendor Cursor

# Dry run (preview without creating files)
nooprompter --dry_run
```

### Discover Tags

``` bash
# Find all tags in configured templates
nooprompter-tags

# Find tags in specific file/directory
nooprompter-tags --path templates/Cursor/project.mdc
```

### Manage Configuration

``` bash
# Show current configuration
nooprompter-config

# Validate configuration
nooprompter-config --validate
```

### Initialize Project

``` bash
# Create new .nooprompter.yml config
nooprompter-init

# Overwrite existing config
nooprompter-init --force
```

## Python API

You can also use nooprompter programmatically:

``` python
from nooprompter.config import load_config
from nooprompter.tags import discover_tags, substitute_tags
from nooprompter.templates import process_all

# Load configuration
config = load_config()

# Discover tags in text
text = "Hello <User-Name>, welcome to <Project-Name>!"
tags_found = discover_tags(text)
print(f"Found tags: {tags_found}")

# Substitute tags
tags = {'User-Name': 'Vic', 'Project-Name': 'Nooprompter'}
result = substitute_tags(text, tags)
print(f"Result: {result}")
```

    2

## Configuration

The `.nooprompter.yml` file configures how templates are processed:

### Basic Structure

``` yaml
# Tags - define your substitutions
tags:
  Project-Name: my-project
  Project-Objective: Describe your project
  User-Alias: developer
  User-Profile: |
    Add your developer profile here

# Templates - define vendors and their destinations
templates:
  # Meta templates (special case - dest hardcoded to meta/)
  meta: {}
  
  # Cursor AI IDE
  # Source convention: templates/Cursor/
  Cursor:
    dest: ".cursor/rules/"
  
  # Claude Coder extension
  # Source convention: templates/Claude_Coder/
  Claude_Coder:
    dest: ".claude-coder/rules/"

# General configuration
general:
  # Files to ignore during processing
  ignore:
    - "*.pyc"
    - "__pycache__"
  
  # Options
  warn_missing_tags: true
  strict_mode: false
  max_recursion: 10
  preserve_timestamps: false
```

### Conventions

- **Template sources**: By convention, templates are in
  `templates/{vendor_name}/`
- **Meta destination**: The `meta` vendor outputs to `meta/` (hardcoded)
- **Other destinations**: Specify with `dest` key for each vendor
- **Default extensions**: `.md`, `.mdc`, `.txt`, `.yml`, `.yaml`

### Tag Features

- **Simple tags**: `<Project-Name>` → `my-project`
- **Nested tags**: Tags can contain other tags, recursively substituted
- **Multi-line tags**: Use YAML’s `|` or `>` for multi-line values
- **Missing tags**: Configurable behavior (warn or leave as-is)

### Template Processing

- Templates discovered recursively in source directories
- Files matching ignore patterns are skipped
- Relative paths preserved in destination
- Destination directories created automatically

## Use Cases

### IDE Rules Management

Maintain a single source of truth for your IDE rules across projects:

    templates/Cursor/project.mdc → .cursor/rules/project.mdc
    templates/Cursor/about-me.mdc → .cursor/rules/about-me.mdc

### Prompt Libraries

Create reusable prompt templates:

    templates/meta/HANDOFF.md → meta/HANDOFF.md
    templates/meta/STARTING_PROMPT.md → meta/STARTING_PROMPT.md

### Multi-Project Documentation

Share common documentation structure across projects with
project-specific tags.

## License

Apache 2.0

## Developer Guide

If you are new to using `nbdev` here are some useful pointers to get you
started.

### Install nooprompter in Development mode

``` sh
# make sure nooprompter package is installed in development mode
$ pip install -e .

# make changes under nbs/ directory
# ...

# compile to have changes apply to nooprompter
$ nbdev_prepare
```
